services:
  frontend:
    image: ghcr.io/entryfra/preview-deployment-frontend:latest

  nginx:
    image: nginx:alpine

    ports:
      - "80:80"

    volumes:
      - ./nginx/default.prod.conf:/etc/nginx/conf.d/default.conf:ro

    depends_on:
      - frontend
